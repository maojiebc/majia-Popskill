# Popskill v2.6.0 — Marketplace 插件可见

## 新增

- **Claude Code Marketplace 插件进矩阵（只读层）**：dbs 这类通过 `/plugin` 安装的插件套装（活在 `~/.claude/plugins/` 自管缓存，此前是 20 个 skill 的盲区）现在以套装卡形式入册——可搜索、可 peek、计入统计，带 **MARKETPLACE** 标识与来源仓库。
- **边界诚实**：插件生命周期归 Claude Code——开关 / 移除 / 更新在三层（UI / 模型 / 引擎）全部守卫，操作提示去 `/plugin`；Claude 列显示已加载，Codex 列如实显示不消费。

「我装了什么」从此一张表答全：store 技能 + 源式套装 + Marketplace 插件。测试 44 → 45。
