# Popskill v2.3.1 — 弹层底色修复

## 修复

- **弹层底色发灰**：添加 / 设置弹层、修复弹层、详情 peek 的容器阴影会被内部每个子行重复投影，把暖纸底色糊成灰色（与原型不一致）。已改为先合成一层再投影（`compositingGroup`），底色恢复 #fafaf8 暖纸、头尾 #f4f2ec 层级正确。
- **frontmatter 多行描述**：`description: |` 块标量不再显示成一个 "|" 字符，正确取首段文本。

测试 36 → 37。
