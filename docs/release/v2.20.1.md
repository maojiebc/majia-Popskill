# v2.20.1 — 折叠套装卡不再被工具列挤成竖排字母

v2.20.0 起，本机已有技能目录的 Grok / Gemini / OpenCode / Pi 会在矩阵里占列。折叠套装卡右侧分数条没有固定列宽，六列挤在半宽卡片里时，SwiftUI 把 `CLAUDE` 拆成竖排字母、和 0/1 叠在一起。

现在每列固定宽度、标签强制单行（OpenCode 写成 OPEN），空间不够只截左侧名称。
