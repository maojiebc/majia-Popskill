# Popskill v2.5.0 — 更新提醒看得见

## 修复

- **App 自身更新提醒从未触发**：两层原因——Sparkle 默认 24 小时才查一次（同日多版本永远赶不上）、且未启用自动检查（默认要等第二次启动的许可弹窗）。现在打包即启用自动检查、间隔 1 小时；设置 → 关于新增「检查 App 更新…」按钮可随时手动查。
- **套装更新不知道具体哪个**：检查发现的变更成员名现在持久化并显示——展开套装后，**有新版的成员行带 ↑ 角标**；套装头部 ↑ 徽标悬停列出完整名单；更新完成的提示也报具体名字。

测试 44/44。
