# v2.20.2 — 首页默认 Claude / Codex / Cursor

矩阵默认只留 Claude Code、Codex、Cursor 三列。Grok、Gemini、OpenCode、Pi 本机有 App 或 CLI 之后，才在设置里出现「首页显示」开关，默认关闭。

折叠套装仍是两列卡片，不拉成通栏条。独立卡在三列及以上时，挂载 pill 改到底栏。

pipx 从 GitHub / 本地装的包（例如 agent-reach 1.5.0）不再跟 PyPI 同名旧包比版本，避免把 0.1.0 当成可升级。
