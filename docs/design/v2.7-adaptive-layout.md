# v2.7 自适应布局（实机驱动演进，无原型包）

用户实机反馈：原型按 1280 固定宽 + 3 套装设计；现实 6+ 套装、窗口拉到 2000pt 后，
折叠套装通栏中间大片空白、双列卡片变胖不变多、套装多了首屏全是横条。

演进决策（用户确认方向后实施）：
1. **自适应列数**：~450pt/卡，1280→2 列、1700→3 列、2100+→4 列
2. **折叠套装入网格**：折叠态收成紧凑卡（名称+BUNDLE 标+N 项+desc+迷你聚合分数），
   与独立卡混排；**「通栏」从套装固有属性变为展开态的临时属性**
3. 展开态布局不变（PATCH-02 定稿的列对齐清单）

视觉语言不变（账本暖纸/发丝线/绿激活）。紧凑卡 = BundleCompactCard。
